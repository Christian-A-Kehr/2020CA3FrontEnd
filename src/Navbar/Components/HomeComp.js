import React from "react";

export default function Home() {
    return (
        <div>
            <h2>Home</h2>
            <p>Don't touch: 'Login/Login.js', 'index.js' and 'ApiFacade.js'. <br />
                All URLs are to be added in 'Settings/Settings.js'. Remember to handle exports and imports of the URLs where required.<br />
                A template for how to use REST fetch is shown in '/REST/Jokes.js'. This can be changed or copied according to the project requirements.<br />
                <br />
                The Navbar is also partly a template. It should be modified to suit the project.<br />
                <br />
                When cloning, remember to double check the REST URLs.<br />
                Then use 'npm install', 'npm install react-router-dom' followed by 'npm start' to get a local deployment.<br />
                Surge deployment is done as follows: ... TBD</p>
        </div>
    );
}